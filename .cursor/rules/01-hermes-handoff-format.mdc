---
description: "Hermes handoff format router"
alwaysApply: true
---

# Hermes Handoff Router

Templates and `SKILL_SUGGESTION` quality contract:

`docs/agent-workflows/hermes-handoff.md`

Markers:

- `PLAN_TO_SKILL_REQUEST` — after Plan Mode, before implementation
- `SKILL_SUGGESTION` — Hermes response with full skill draft
- `SKILL_APPLICATION_SUMMARY` — after implementation
- `SKILL_IMPROVEMENT_REQUEST` — when skill needs update

Validation commands: `AGENTS.md` § Validation only. Do not invent generic `pytest` / `mypy .` unless listed there.

Skill trust: `docs/agent-workflows/skill-trust-boundary.md`

Workflow: `.cursor/rules/00-hermes-skill-suggestion.mdc`
