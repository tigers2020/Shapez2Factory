# {{PROJECT_NAME}} MCP

Per AGENTS.md, assess whether MCP usable before related work. No MCP available → fall back to local files + standard verification, record that fact.

## context7

- Prioritize when latest docs needed, e.g. new libraries or framework API usage.
- Don't copy external API examples verbatim; separate to match project layer boundaries into application ports + adapters.

## GitLens

- Consider when change history, regression root cause, PR review prep, or branch analysis needed.
- Confirm history-based inference together with current file contents.

## cursor-ide-browser

- Use when UI visual verification, web doc validation, or browser interaction checks needed.

## Schema verification

- Where `call_mcp_tool` / `fetch_mcp_resource` invoked directly, check `mcps/<server>/tools/` schema first.
- Tool missing or fails → record failure reason, use local alternative.
