---
description: "Root router: AGENTS.md is canon; .mdc files route"
alwaysApply: true
---

# Root Router

Canon: `AGENTS.md`.

**AGENTS.md controls HOW. Spec/ADR controls WHAT.** See `AGENTS.md` § Authority split.

Route when relevant:

- Scope: `agent_scope.mdc`
- Phases (Normal+): `workflow-phases.mdc` (`workflow-phases.md`)
- Kanban WIP: `kanban-tracking.mdc` (`kanban-tracking.md`)
- Strictness / SDD: `workflow.mdc` (`workflow-strictness.md`)
- Ops/recovery: `ops-recovery.mdc`
- Shapez2: `shapez2-domain.mdc`, `shapez2-core.mdc`
- Code search (graph exists): `graphify.mdc`
- UI/browser verification: `playwright.mdc`
- Git hygiene: `git-worktree.mdc`
- Delivery safety: `workflow-safety.mdc` (`workflow-safety.md`)
- Style: `caveman.mdc`

Do not claim done without evidence. Do not commit/push/PR/branch unless user asks (`workflow-safety.mdc`).
