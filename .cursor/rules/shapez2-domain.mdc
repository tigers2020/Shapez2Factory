---
description: "Shapez2 router: grill + Hermes pipeline; current canon wins"
alwaysApply: true
---

# Shapez2 Domain Router

Canon: `AGENTS.md` § Shapez2 Routing.

Trigger `/grill-me-shapez2` when task touches Shapez2, Asteroid Lab, solver layers, mining, placement, routing, replay, UI, reconstruction, transport, belt/pipe, L2/L3/L4, rim placement, DTO/replay contracts, or related code.

## Canonical pipeline

Full order: `docs/agent-workflows/hermes-skill-suggestion.md`

```text
Problem → contract check → Plan Mode → grill (if ambiguous) → PLAN_TO_SKILL_REQUEST
→ SKILL_SUGGESTION → implement → SKILL_APPLICATION_SUMMARY
```

Grill only when domain contract is ambiguous, solver behavior changes, or user asks. If grill and Hermes conflict: **tell the user** — do not silently choose.

Current canon/spec/ADR beats stale docs, archived notes, and agent memory.

Related: `asteroid-lab-invariants.mdc`, `django-apps.mdc`, `web-interface.mdc`, `architecture.mdc`, `workflow.mdc`
