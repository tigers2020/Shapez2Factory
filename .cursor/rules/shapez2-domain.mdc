---
description: "Shapez2 router: canon and invariants"
alwaysApply: true
---

# Shapez2 Domain Router

Canon: `AGENTS.md` § Shapez2.

Applies when task touches Shapez2, Asteroid Lab, solver layers, mining, placement, routing, replay, UI, reconstruction, transport, belt/pipe, L2–L5, DTO/replay contracts.

- Current canon/spec/ADR beats stale docs and agent memory.
- Architecture/coupling: `graphify` when `graphify-out/graph.json` exists (`graphify.mdc`).
- Invariants on glob match: `asteroid-lab-invariants.mdc`.

Related: `shapez2-core.mdc`, `workflow.mdc`, `django-apps.mdc`, `web-interface.mdc`.
