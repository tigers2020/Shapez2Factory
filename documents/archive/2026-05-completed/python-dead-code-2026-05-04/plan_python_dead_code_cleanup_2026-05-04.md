# Python 죽은·중복·레거시 코드 정리 — 실행 플랜 (2026-05-04)

## 삭제 vs 격리 기준 (플랜 정본)

| 판단 | 조치 |
|------|------|
| 테스트·URL·다른 앱에서 참조 없음, 도메인 의미 없음 | **삭제** + 회귀 테스트 |
| 참조 없으나 보존 가치 있음 | `django_apps/.../_deprecated/` 등으로 **이동** + docstring 한 줄 |
| 불확실(리플렉션·문자열 로딩) | **유지** + 리서치에 보류 사유 |

레이어 규칙: `architecture.mdc` 준수(deprecated도 동일 레이어 내).

## 이번 1차 구현 대상 (조사 결과 반영)

1. **Ruff I001**: `ruff check --fix`로 정렬 가능 구간 적용.
2. **Ruff E501**: 위 표의 4곳을 줄바꿈·import 분할로 100자 이내.
3. **모듈 삭제·`_deprecated` 이동**: 조사 시점에서 **전체 미사용 모듈로 확정된 항목 없음** — 별도 격리 없음. (Vulture 미실행으로 추가 후보는 추후 선택 설치 후 2차 검토 가능.)

## 사람 승인

- [x] **승인**: 사용자가 Cursor에서 본 플랜의 구현을 직접 지시함 (2026-05-04). 구현 단계 진입 허용.

## 검증 (구현 후)

- `python -m pytest` (전체 또는 변경 영향 구간)
- `ruff check .` → `mypy .` → `black .` (CI는 `black --check .`)

**갱신(후속 진행)**: `mypy .`까지 통과하도록 웹 allauth 연동 모듈 override·`context_processors` 타입·테스트 `dict` 제네릭을 반영함.
