# Open Schema Questions

| Topic | Question | Owner |
| ----- | -------- | ----- |
| `building_variant_custom_config` | Structured columns vs deferred audit for `CustomData.All[]`? | Needs review |
| `progression_layout_index` | Import `ResearchUnlockManager` layout indices? | Needs review |
| `ResearchPrerequisite` | Polymorphic parent (`milestone` vs `side_quest`) — single parent FK table? | Needs review |
| `ResearchUnlockReward` | Reward `$type` variants not fully enum-mapped in v1 | Needs review |
| `LocalizedMessage` | Re-export required; resolve LazyText from other dumps? | Blocked on data |
| `connectable_simulation_attachment` | Extract `building_kind_key` vs audit blob only? | Needs review |
| `building_group` duplicate import | Same `group_key` from buildings + groups — lazy profile wins if groups imported last | Documented |
