/** Iterations for cross-layer ordering passes. */
export const ORDERING_PASSES = 4;

/** Iterations for node position relaxation. */
export const POSITIONING_PASSES = 6;

/** localStorage / sessionStorage key for opt-in layout debug logs. */
export const EDITOR_LAYOUT_CONSOLE_DEBUG_LS_KEY = "shapezDebugGraphLayout";
